APP_WORKSPACE="${APP_WORKSPACE:-app/Shorty.xcworkspace}"
APP_SCHEME="${APP_SCHEME:-Shorty}"
APP_IDENTIFIER="${APP_IDENTIFIER:-app.peyton.shorty}"
APP_PRODUCT_NAME="${APP_PRODUCT_NAME:-Shorty}"
TEST_SCHEME="${TEST_SCHEME:-ShortyCore}"
BRIDGE_SCHEME="${BRIDGE_SCHEME:-ShortyBridge}"
SCREENSHOT_SCHEME="${SCREENSHOT_SCHEME:-ShortyScreenshots}"

BUILD_DERIVED_DATA="${BUILD_DERIVED_DATA:-.DerivedData/build}"
RUN_DERIVED_DATA="${RUN_DERIVED_DATA:-.DerivedData/run}"
TEST_DERIVED_DATA="${TEST_DERIVED_DATA:-.DerivedData/test}"
BRIDGE_DERIVED_DATA="${BRIDGE_DERIVED_DATA:-.DerivedData/bridge}"
SCREENSHOT_DERIVED_DATA="${SCREENSHOT_DERIVED_DATA:-.DerivedData/screenshots}"

TEAM_ID="${TEAM_ID:-3VDQ4656LX}"
